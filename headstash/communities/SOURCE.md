# Community snapshots

Restored from [hard-nett/airdrop](https://github.com/hard-nett/airdrop) tree `cea3a87` (last commit that still had the CSVs).

Workspace pin requested: `b432fa6c248b284110cfaf637d3af44b3ebac5d9` — that commit had already **deleted** these files. YAML paths in `circuit/headstash.yaml` still point here:

`crates/headstash/headstash/communities/<project>/<project>.csv`
